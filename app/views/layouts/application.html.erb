<!DOCTYPE html>
<html>
  <head>
    <title>HomeConstructionManagement</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <header class="col-md-12 mt-1">
      <nav class="navbar navbar-default bg-warning">
        <div class="navbar-header m-2 ms-2">
          <%= link_to 'Home Construction Management', root_path, class: 'navbar-brand link-light' %>
        </div>
      </nav>
    </header>
    <div class="container">
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
      <div class="row">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
